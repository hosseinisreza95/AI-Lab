openai>=1.40.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
langgraph>=0.2.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
pandas>=2.0.0
numpy>=1.26.0
pyarrow>=15.0.0
scikit-learn>=1.4.0
joblib>=1.3.0
rank-bm25>=0.2.2
PyYAML>=6.0
python-dotenv>=1.0.0
pytest>=8.0.0

# Optional: the Spark engine. Needs a JVM (JAVA_HOME set).
# The pipeline falls back to the pandas engine without it.
# pyspark>=3.5.0

# Optional: only needed to run the DAG in a real Airflow installation.
# apache-airflow>=2.9.0
