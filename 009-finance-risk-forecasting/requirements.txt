pandas>=2.0.0
numpy>=1.26.0
pyarrow>=15.0.0
scikit-learn>=1.4.0
statsmodels>=0.14.0
scipy>=1.11.0
joblib>=1.3.0
pytest>=8.0.0

# Optional: only needed for the LSTM comparison in models/forecasting.py.
# Without it the backtest runs SARIMA and the baselines and says so.
# torch>=2.0.0
